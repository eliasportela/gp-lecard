{
  "name": "gp-lecard",
  "version": "1.6.5",
  "description": "Gerenciador de Pedidos do LeCard Delivery",
  "author": "storkdigital.com.br",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder --ia32",
    "dist": "electron-builder --ia32 --publish always"
  },
  "build": {
    "asar": "true",
    "productName": "LeCard - Gestor de Pedidos",
    "appId": "delivery.lecard.gplecard",
    "artifactName": "gestor-lecard-${version}.${ext}"
  },
  "license": "ISC",
  "devDependencies": {
    "electron": "^13.6.0",
    "electron-builder": "^22.10.5"
  },
  "dependencies": {
    "electron-updater": "^4.3.9"
  }
}
